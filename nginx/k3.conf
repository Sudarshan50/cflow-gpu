# Upstreams only. The public edge is k3-tls.conf; the plaintext :8000 edge was
# removed when the API moved to TLS.
upstream k3_backend { server 127.0.0.1:8001; keepalive 32; }
upstream k3_dash    { server 127.0.0.1:8080; keepalive 8; }
